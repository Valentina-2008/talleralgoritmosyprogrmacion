estudiantes = {}
num = (input("cantidad:"))
for num in range:
    estudiante = input("nombre:")
    for estudiante in estudiantes:
        estudiante = input("nombre:")
    ns=[]
    n= (input("nota:"))
    while n > 0:
        ns.append(n)
        print (ns)


